# 英文單字背誦系統 - SQLite 資料庫版本新手教學

## 📚 目錄
1. [什麼是 SQLite？](#什麼是-sqlite)
2. [為什麼要用資料庫？](#為什麼要用資料庫)
3. [SQL 基礎知識](#sql-基礎知識)
4. [程式架構說明](#程式架構說明)
5. [重要概念解釋](#重要概念解釋)
6. [使用教學](#使用教學)
7. [常見問題](#常見問題)

---

## 什麼是 SQLite？

### 簡單來說
SQLite 是一個「輕量級的資料庫」，就像是一個超級厲害的 Excel，但功能更強大：
- 📁 所有資料儲存在一個檔案裡（例如：`vocabulary.db`）
- 🚀 查詢速度比文字檔快很多
- 🔒 資料更安全，不容易損毀
- 💪 Python 內建支援，不需要額外安裝

### 跟文字檔的差異

**文字檔版本：**
```
unit1	apple	蘋果	0
unit1	banana	香蕉	2
```
- ❌ 要讀取整個檔案才能找資料
- ❌ 資料格式容易出錯
- ❌ 難以處理複雜的查詢

**SQLite 版本：**
```sql
SELECT * FROM words WHERE folder = 'unit1';
```
- ✅ 只讀取需要的資料（超快！）
- ✅ 資料格式嚴格，不容易出錯
- ✅ 可以做複雜的搜尋和統計

---

## 為什麼要用資料庫？

### 1. 速度更快 ⚡
```python
# 文字檔：要讀完整個檔案
for line in file:
    if "apple" in line:  # 每一行都要檢查
        print(line)

# SQLite：直接找到目標
cursor.execute("SELECT * FROM words WHERE english = 'apple'")
# 使用「索引」，就像書的目錄，直接翻到那一頁
```

### 2. 資料更安全 🔒
- 文字檔：如果程式當機，可能寫到一半就損毀
- SQLite：有「交易機制」，要嘛全部成功，要嘛全部取消

### 3. 功能更強大 💪
```sql
-- 統計每個資料夾有幾個單字（一行搞定！）
SELECT folder, COUNT(*) FROM words GROUP BY folder;

-- 找出錯誤最多的前 5 個單字
SELECT * FROM words ORDER BY error_count DESC LIMIT 5;

-- 計算總錯誤次數
SELECT SUM(error_count) FROM words;
```

---

## SQL 基礎知識

### 什麼是 SQL？
SQL = Structured Query Language（結構化查詢語言）
就是用來跟資料庫「對話」的語言。

### 四個最重要的指令（CRUD）

#### 1. CREATE（建立）- 建立資料表
```sql
CREATE TABLE words (
    id INTEGER PRIMARY KEY AUTOINCREMENT,  -- 編號（自動增加）
    english TEXT NOT NULL,                 -- 英文單字（不能空白）
    chinese TEXT NOT NULL,                 -- 中文意思（不能空白）
    folder TEXT NOT NULL,                  -- 資料夾名稱（不能空白）
    error_count INTEGER DEFAULT 0          -- 錯誤次數（預設 0）
);
```

**重要名詞解釋：**
- `PRIMARY KEY`：主鍵，每筆資料的唯一識別碼（就像身分證字號）
- `AUTOINCREMENT`：自動遞增，新增資料時會自動給一個新的編號
- `NOT NULL`：不能是空值
- `DEFAULT 0`：預設值是 0

#### 2. INSERT（插入）- 新增資料
```sql
-- 新增一個單字
INSERT INTO words (english, chinese, folder, error_count)
VALUES ('apple', '蘋果', 'unit1', 0);

-- 同時新增多個單字
INSERT INTO words (english, chinese, folder, error_count)
VALUES 
    ('banana', '香蕉', 'unit1', 0),
    ('orange', '橘子', 'unit1', 0);
```

#### 3. SELECT（選取）- 查詢資料
```sql
-- 查詢所有資料
SELECT * FROM words;

-- 查詢特定欄位
SELECT english, chinese FROM words;

-- 條件查詢（WHERE）
SELECT * FROM words WHERE folder = 'unit1';

-- 模糊搜尋（LIKE）
SELECT * FROM words WHERE english LIKE '%app%';
-- % 代表任意字元，所以 'apple', 'pineapple' 都會被找到

-- 排序（ORDER BY）
SELECT * FROM words ORDER BY english;          -- 按英文排序
SELECT * FROM words ORDER BY error_count DESC; -- 按錯誤次數降序

-- 限制數量（LIMIT）
SELECT * FROM words LIMIT 10;  -- 只取前 10 筆
```

#### 4. UPDATE（更新）- 修改資料
```sql
-- 更新錯誤次數
UPDATE words
SET error_count = 5
WHERE id = 1;

-- 同時更新多個欄位
UPDATE words
SET english = 'apple', chinese = '蘋果'
WHERE id = 1;
```

#### 5. DELETE（刪除）- 刪除資料
```sql
-- 刪除特定單字
DELETE FROM words WHERE id = 1;

-- 刪除整個資料夾的單字
DELETE FROM words WHERE folder = 'unit1';

-- 刪除所有資料（小心使用！）
DELETE FROM words;
```

### 進階查詢

#### 統計函數
```sql
-- COUNT：計算數量
SELECT COUNT(*) FROM words;                    -- 總單字數
SELECT COUNT(*) FROM words WHERE folder = 'unit1';  -- unit1 有幾個單字

-- SUM：加總
SELECT SUM(error_count) FROM words;            -- 總錯誤次數

-- AVG：平均
SELECT AVG(error_count) FROM words;            -- 平均錯誤次數

-- MAX / MIN：最大值 / 最小值
SELECT MAX(error_count) FROM words;            -- 最多錯幾次
```

#### 分組（GROUP BY）
```sql
-- 統計每個資料夾有幾個單字
SELECT folder, COUNT(*) as count
FROM words
GROUP BY folder;

-- 結果：
-- unit1  | 5
-- unit2  | 3
```

#### 去除重複（DISTINCT）
```sql
-- 取得所有不重複的資料夾名稱
SELECT DISTINCT folder FROM words;
```

---

## 程式架構說明

### 三層架構

```
┌─────────────────────────────────────┐
│   使用者介面層（VocabularySystem）    │  ← 使用者看到的選單和互動
│   - 顯示選單                          │
│   - 接收使用者輸入                     │
│   - 顯示結果                          │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   業務邏輯層（VocabularySystem）      │  ← 處理功能邏輯
│   - add_word_interface()            │
│   - take_test()                     │
│   - show_flashcards()              │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   資料存取層（VocabularyDatabase）    │  ← 直接操作資料庫
│   - add_word()                      │
│   - get_all_words()                │
│   - update_error_count()           │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│        SQLite 資料庫檔案              │  ← vocabulary.db
└─────────────────────────────────────┘
```

### 為什麼要分層？

**優點：**
1. **關注點分離**：每一層只負責自己的工作
2. **容易維護**：要修改資料庫操作，只要改資料存取層
3. **容易測試**：可以單獨測試每一層
4. **容易擴展**：想換成 MySQL？只要改資料存取層

**範例：**
```python
# 使用者介面層：不需要知道資料庫細節
def take_test(self):
    words = self.db.get_all_words()  # 只要呼叫方法
    # ... 測驗邏輯

# 資料存取層：不需要知道測驗邏輯
def get_all_words(self):
    self.cursor.execute("SELECT * FROM words")
    # ... 資料庫操作
```

---

## 重要概念解釋

### 1. 連線（Connection）與游標（Cursor）

**連線（Connection）**
```python
connection = sqlite3.connect("vocabulary.db")
```
就像是「打電話給資料庫」，建立一個溝通管道。

**游標（Cursor）**
```python
cursor = connection.cursor()
```
就像是「打字機的游標」，指向目前要操作的位置。
我們透過游標來執行 SQL 指令。

```python
# 完整流程
connection = sqlite3.connect("vocabulary.db")  # 1. 連線
cursor = connection.cursor()                   # 2. 建立游標
cursor.execute("SELECT * FROM words")          # 3. 執行指令
results = cursor.fetchall()                    # 4. 取得結果
connection.commit()                            # 5. 提交變更
connection.close()                             # 6. 關閉連線
```

### 2. 提交（Commit）

**為什麼需要 commit？**
```python
# 新增資料
cursor.execute("INSERT INTO words VALUES (...)")
# 此時資料只在「暫存區」，還沒真正寫入

connection.commit()  # 正式寫入資料庫
# 如果程式在 commit 前當機，資料不會被寫入（保護機制）
```

這就像是 Word 的「儲存」功能：
- 沒按儲存 = 沒 commit → 當機後資料消失
- 按了儲存 = commit → 資料安全存檔

### 3. 參數化查詢（防止 SQL 注入）

**❌ 錯誤做法（危險！）**
```python
english = input("輸入單字: ")
# 如果使用者輸入: apple'; DROP TABLE words; --
# 會變成: SELECT * FROM words WHERE english = 'apple'; DROP TABLE words; --
# 你的資料表就被刪掉了！

sql = f"SELECT * FROM words WHERE english = '{english}'"
cursor.execute(sql)
```

**✅ 正確做法（安全）**
```python
english = input("輸入單字: ")
# 使用 ? 參數佔位符
cursor.execute("SELECT * FROM words WHERE english = ?", (english,))
# SQLite 會自動處理特殊字元，不會被注入攻擊
```

### 4. 索引（Index）

**什麼是索引？**
就像是書的「目錄」或「索引頁」，可以快速找到資料。

**沒有索引：**
```
找 "apple" → 從第 1 頁翻到最後一頁（慢！）
```

**有索引：**
```
找 "apple" → 查目錄 → 直接翻到第 23 頁（快！）
```

**建立索引：**
```python
# 在 folder 欄位建立索引
cursor.execute("CREATE INDEX idx_folder ON words(folder)")

# 之後查詢就會快很多
cursor.execute("SELECT * FROM words WHERE folder = 'unit1'")
```

**注意：**
- 索引會加快查詢速度
- 但會增加新增/修改資料的時間（因為要更新索引）
- 通常在「常常查詢」的欄位建立索引

### 5. 物件導向（OOP）

**類別（Class）**
就像是「設計圖」，定義物件有哪些屬性和功能。

**物件（Object）**
根據設計圖製造出來的「實體」。

**範例：**
```python
# 類別定義（設計圖）
class Word:
    def __init__(self, english, chinese):
        self.english = english
        self.chinese = chinese
    
    def show(self):
        print(f"{self.english} - {self.chinese}")

# 建立物件（製造實體）
word1 = Word("apple", "蘋果")
word2 = Word("banana", "香蕉")

# 使用物件
word1.show()  # 輸出：apple - 蘋果
word2.show()  # 輸出：banana - 香蕉
```

**為什麼要用物件導向？**
1. **封裝**：把相關的資料和功能包在一起
2. **重用**：可以重複使用類別來建立很多物件
3. **維護**：程式碼結構清楚，容易維護

---

## 使用教學

### 安裝與執行

1. **確認 Python 版本**
   ```bash
   python --version
   # 需要 Python 3.6 以上
   ```

2. **執行程式**
   ```bash
   python vocabulary_system_sqlite.py
   ```

3. **第一次執行**
   - 會自動建立 `vocabulary.db` 檔案
   - 會建立 `words` 資料表
   - 會建立索引

### 功能說明

#### 1. 新增單字
```
請輸入要存入的資料夾名稱: unit1
請輸入格式: [英文單字] [空格或Tab] [中文意思]
>apple 蘋果
[成功] 已新增單字: apple - 蘋果
>banana 香蕉
[成功] 已新增單字: banana - 香蕉
>end
結束新增單字。
```

#### 2. 單字卡學習
- 選擇資料夾或全部單字
- 系統會隨機出題
- 先顯示英文，按 Enter 後顯示中文
- 輸入 'q' 可以離開

#### 3. 開始測驗
- 選擇資料夾或全部單字
- 系統顯示中文，你輸入英文
- 答錯會自動記錄錯誤次數
- 測驗結束會顯示結果和錯題清單

#### 4. 錯題本
- 顯示所有錯過的單字（按錯誤次數排序）
- 可以直接進入複習模式

#### 5. 查詢單字
- 輸入中文或英文關鍵字
- 支援模糊搜尋
- 會顯示所有符合的單字

#### 6. 統計資訊
- 顯示單字總數
- 顯示資料夾數量
- 顯示各資料夾的單字數量
- 顯示錯誤統計

#### 7. 刪除單字
- 先搜尋單字
- 選擇要刪除的項目
- 需要確認後才會刪除

---

## 常見問題

### Q1: 資料庫檔案在哪裡？
**A:** 在程式同一個目錄下，檔名是 `vocabulary.db`

### Q2: 可以用什麼軟體打開資料庫檔案？
**A:** 可以使用以下工具：
- [DB Browser for SQLite](https://sqlitebrowser.org/)（免費，推薦）
- [SQLite Viewer](https://inloop.github.io/sqlite-viewer/)（網頁版）
- [DBeaver](https://dbeaver.io/)（功能強大）

### Q3: 如何備份資料？
**A:** 複製 `vocabulary.db` 檔案即可！

### Q4: 如何匯入舊的文字檔資料？
**A:** 可以寫一個簡單的轉換程式：

```python
import sqlite3

# 連接資料庫
conn = sqlite3.connect("vocabulary.db")
cursor = conn.cursor()

# 讀取舊的文字檔
with open("english_word.txt", "r", encoding="utf-8") as f:
    for line in f:
        parts = line.strip().split('\t')
        if len(parts) >= 3:
            folder, english, chinese = parts[0], parts[1], parts[2]
            error_count = int(parts[3]) if len(parts) >= 4 else 0
            
            cursor.execute("""
                INSERT INTO words (folder, english, chinese, error_count)
                VALUES (?, ?, ?, ?)
            """, (folder, english, chinese, error_count))

conn.commit()
conn.close()
print("匯入完成！")
```

### Q5: 如何匯出成文字檔？
**A:** 

```python
import sqlite3

conn = sqlite3.connect("vocabulary.db")
cursor = conn.cursor()

cursor.execute("SELECT folder, english, chinese, error_count FROM words")
rows = cursor.fetchall()

with open("export.txt", "w", encoding="utf-8") as f:
    for row in rows:
        f.write(f"{row[0]}\t{row[1]}\t{row[2]}\t{row[3]}\n")

conn.close()
print("匯出完成！")
```

### Q6: 程式可以多人使用嗎？
**A:** 目前版本設計給單人使用。
如果要多人使用，需要：
1. 使用 MySQL 或 PostgreSQL（支援多人同時連線）
2. 加入使用者登入系統
3. 處理並發（Concurrency）問題

### Q7: 如何重置所有錯誤次數？
**A:** 執行以下 SQL：

```python
cursor.execute("UPDATE words SET error_count = 0")
connection.commit()
```

或是手動加入「重置功能」：

```python
def reset_all_errors(self):
    try:
        self.cursor.execute("UPDATE words SET error_count = 0")
        self.connection.commit()
        print("已重置所有錯誤次數。")
    except sqlite3.Error as e:
        print(f"重置失敗: {e}")
```

---

## 學習資源

### Python SQLite 教學
- [官方文件](https://docs.python.org/zh-tw/3/library/sqlite3.html)
- [SQLite Tutorial](https://www.sqlitetutorial.net/)
- [W3Schools SQL](https://www.w3schools.com/sql/)

### 進階主題
- 交易（Transaction）
- 外鍵（Foreign Key）
- 觸發器（Trigger）
- 視圖（View）
- 正規化（Normalization）

---

## 總結

這個程式示範了：
✅ 如何使用 SQLite 資料庫  
✅ 如何執行 CRUD 操作  
✅ 如何設計三層架構  
✅ 如何防止 SQL 注入  
✅ 如何建立索引提升效能  
✅ 如何使用物件導向設計  

希望這個教學能幫助你理解資料庫的基本概念！🎉
