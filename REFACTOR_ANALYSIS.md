# 英文單字網站分析與重構方案

日期：2026-09-11。範圍：本地程式碼與 `tests/`；未驗證正式站畫面、正式環境或真實 AI API。

## 已完成：建立可重複的測試基礎

原有 6 項測試在隔離工作目錄執行，結果為 5 通過、1 失敗。登入測試傳送舊版 `email` 欄位，但現行代理接受 `username` 或 `username_or_email`。原測試在匯入 `web` 後更改環境變數，無法改變匯入時固定的 `web.DB_NAME`；不同測試可能連到同一個開發資料庫，空列表測試也未檢查實際內容。

本次只重構測試基礎：共用 fixture 為每項測試建立暫存資料庫、切換暫存工作目錄、還原環境設定與 Flask 設定、清除統計快取，並阻止未模擬的 requests HTTP。認證測試模擬 auth 回應，驗證請求格式、使用者快取、登入登出、401、409、503 與必要欄位。API 測試驗證新增後可讀取、刪除後清空、拒絕未認證寫入，以及連線關閉。未改動正式程式、DB schema 或跨服務契約。

驗證：`/home/ubuntu/nex11/word/.venv/bin/python -m pytest /home/ubuntu/nex11/word/tests -q`，13 項通過。這些測試尚未涵蓋完整 JWT、OAuth、SRS、AI 或瀏覽器流程。根目錄其他 `test_*.py` 屬整合腳本，本次未執行。

## 優先問題

| 優先 | 程式證據 | 影響與下一步 |
|---|---|---|
| 高 | `web.py` 的 `add_word`、`get_words`、`delete_word` 未按 owner_id 限制；CSV 與部分構詞路由卻有使用 owner_id | 使用者資料範圍不一致。先決定共用詞庫與個人資料的界線，再建立雙使用者存取測試，避免直接改動既有資料語義。 |
| 高 | `require_auth` 接受有效 CSRF；並非所有寫入路由都再檢查登入使用者 | CSRF 驗證與身分驗證混合。需明確定義訪客可做的操作，將登入要求與請求防偽分離。 |
| 高 | 登入註冊代理 auth，但 `change_password` 寫本地 users；本地 OAuth 仍有建立使用者邏輯 | 帳號來源與 SSO 指引不一致。本地修改密碼無法更新 auth 的密碼；須先規劃跨服務處理，再實作。 |
| 高 | `wsgi.py` 在 load_dotenv 前匯入 web；web 在匯入時固定 DB_NAME；AIServiceManager 預設 vocabulary.db | 不同元件可能使用不同設定或資料庫。應統一載入順序與資料庫來源，加入自訂 DB_NAME 的启动與讀寫測試。 |
| 中 | `web.py` 約 1,991 行、`english_word.py` 約 1,463 行、`static/app.js` 約 971 行 | 路由、資料查詢、SRS、認證與 AI 交織；應依下列階段拆分，逐項保持回應格式。 |
| 中 | 多個 AI 路由例外處理使用 `logger.error`，但 web 未定義 logger | 原始錯誤可能被 NameError 遮蔽；可作為下一個獨立小修復並補錯誤路徑測試。 |
| 中 | `get_words` 將輸入直接作 FTS MATCH 表達式，未處理查詢語法錯誤 | 某些一般搜尋輸入可能造成錯誤；補含引號、標點等測試，再決定字面搜尋或進階語法。 |
| 中 | `add_morphology_to_words` 逐字讀取構詞資訊 | 列表查詢量隨單字數增加；批次查詢前先量測與補相同結果測試。 |

以上為程式碼檢查發現；沒有對正式站進行利用測試，也沒有量測正式流量效能。

## 分階段執行

1. **驗證基礎（本次完成）**：隔離測試、校正 auth 模擬與 CRUD 斷言。
2. **局部穩定性修復**：每次處理一項 logger、搜尋錯誤或啟動設定問題，測試後提交並推送。
3. **架構規劃**：依 AGENTS.md，涉及模組邊界、資料表或跨服務契約的大改，先形成具體計畫。建議 Flask application factory 管理設定與資源；Blueprint 分開 words、study、auth、AI；repository 集中資料查詢；service 層處理 SRS 與學習流程。前端再依詞庫、複習、帳號拆成 ES modules。
4. **個人學習資料設計**：先確認詞庫是否共用。如果共用，個人錯題、複習進度應獨立於單字本體；若私人，則各讀寫路由一致驗證所有權。資料遷移需備份、演練及回復方案。
5. **操作流程與畫面**：在功能與資料範圍確認後，驗證手機版瀏覽、搜尋、新增、複習及登入流程，再調整外觀。

## 版本控制狀態

word/ 是獨立 Git repository，遠端為 https://github.com/yh11011/English_Word.git。工作目錄已有 web.py 修改及其他未追蹤檔案；本次僅提交指定測試與報告，不包含既有程式修改或環境檔。13 項通過是針對目前本地工作目錄的結果；由於部分既有執行依賴尚未納入版本控制，不能宣稱乾淨 clone 已可重現。後續應先盤點既有變更與執行依賴，再繼續架構重構。
